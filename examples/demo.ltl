// examples/demo.ltl — runnable demo for ltl_mandelbrot

import std::io
use ltl_mandelbrot as lib

fn main() {
    io::println("ltl_mandelbrot demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}